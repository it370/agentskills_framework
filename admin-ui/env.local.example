# Admin UI - Environment Configuration Template
# Create this file as '.env.local' in the admin-ui directory

# =============================================================================
# REAL-TIME BROADCASTER CONFIGURATION
# =============================================================================

# Broadcaster type: 'pusher' or 'appsync'
NEXT_PUBLIC_BROADCASTER_TYPE=pusher

# --- Pusher Configuration (if BROADCASTER_TYPE=pusher) ---
# Pusher public key (safe to expose to frontend)
# Get from https://dashboard.pusher.com → Your App → App Keys
NEXT_PUBLIC_PUSHER_KEY=your_pusher_key_here

# Pusher cluster (must match backend configuration)
NEXT_PUBLIC_PUSHER_CLUSTER=ap2

# --- AWS AppSync Event API Configuration (if BROADCASTER_TYPE=appsync) ---
# AppSync Event API URL (format: https://{api-id}.appsync-api.{region}.amazonaws.com/event)
# Get from AWS AppSync console
NEXT_PUBLIC_APPSYNC_API_URL=https://your-api-id.appsync-api.us-east-1.amazonaws.com/event
NEXT_PUBLIC_APPSYNC_API_ID=your-api-id
NEXT_PUBLIC_APPSYNC_REGION=us-east-1
# AppSync API Key (REQUIRED - get from AppSync console → Settings → API Keys)
NEXT_PUBLIC_APPSYNC_API_KEY=da2-xxxxxxxxxxxxxxxxxxxxxxxxxx
# AppSync namespace (default: 'default')
NEXT_PUBLIC_APPSYNC_NAMESPACE=default

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Backend API base URL
NEXT_PUBLIC_API_BASE=http://localhost:8000

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file to '.env.local' in the admin-ui directory
# 2. Only use NEXT_PUBLIC_* prefix (required by Next.js for browser access)
# 3. Never expose backend secrets (PUSHER_SECRET, AWS_SECRET_ACCESS_KEY) in frontend
# 4. Restart 'npm run dev' after changing these values
# 5. For production, update NEXT_PUBLIC_API_BASE to your domain
