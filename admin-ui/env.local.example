# Admin UI - Environment Configuration Template
# Create this file as '.env.local' in the admin-ui directory

# =============================================================================
# PUSHER CONFIGURATION (Real-time Broadcasting)
# =============================================================================

# Pusher public key (safe to expose to frontend)
# Get from https://dashboard.pusher.com → Your App → App Keys
NEXT_PUBLIC_PUSHER_KEY=your_pusher_key_here

# Pusher cluster (must match backend configuration)
NEXT_PUBLIC_PUSHER_CLUSTER=ap2

# Enable Pusher (set to 'true' to use Pusher, 'false' for Socket.IO legacy)
NEXT_PUBLIC_USE_PUSHER=true

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Backend API base URL
NEXT_PUBLIC_API_BASE=http://localhost:8000

# =============================================================================
# LEGACY SOCKET.IO (Optional - for backward compatibility)
# =============================================================================

# If using Socket.IO instead of Pusher, configure this:
# NEXT_PUBLIC_SOCKETIO_BASE=http://localhost:7000
# NEXT_PUBLIC_USE_PUSHER=false

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file to '.env.local' in the admin-ui directory
# 2. Only use NEXT_PUBLIC_* prefix (required by Next.js for browser access)
# 3. Never expose PUSHER_SECRET in frontend (keep it backend-only)
# 4. Restart 'npm run dev' after changing these values
# 5. For production, update NEXT_PUBLIC_API_BASE to your domain
